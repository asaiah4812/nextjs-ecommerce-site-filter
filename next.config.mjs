/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["m.media-amazon.com", "example.com" /* Add other domains here */],
    },

};




export default nextConfig;
